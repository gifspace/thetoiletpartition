# Current status

The website source and currently referenced local runtime assets are prepared for commit. PDFs remain excluded; `revise-1/` and `revise-2/` images are included where the pages reference them.
The current hosting package is `export/thetoiletpartition-hosting-2026-09-03.zip` (3,136 ZIP entries, approximately 491.3 MB, no PDF/ZIP entries). It predates the latest 50px Works Hero adjustment, so regenerate the package before hosting upload if that adjustment is required.
